# Zaleznosci runtime + build konwertera danych.
# Instalacja:  pip install -r requirements.txt

pyyaml          # obsluga formatu YAML (Task4/5)
xmltodict       # obsluga formatu XML  (Task6/7)
PySide6         # GUI (Qt) — Task8/9; pakuje sie do samodzielnego .exe/ELF (dziala pod wine)
pyinstaller     # budowanie pliku .exe

# UWAGA — uruchomienie GUI NATYWNIE na Linux wymaga systemowej biblioteki:
#   sudo apt install -y libxcb-cursor0
# (potrzebna przez plugin Qt 'xcb'). Zbudowany .exe na Windows/wine jej NIE potrzebuje.
